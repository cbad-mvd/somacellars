{% extends "matrix/default" %}

{% block blockContent %}
    <blockquote>
        {{ block.quote }}
        {% if block.citation != '' %}
            <cite>{{ block.citation }}</cite>
        {% endif %}
    </blockquote>
{# image display code goes here #}
{% endblock %}
